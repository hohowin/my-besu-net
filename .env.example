# Copy to .env.local and fill in — .env.local is gitignored, never commit real values here.
# These are throwaway demo keys for a local, zero-gas, permissioned network only (D-15).
# Generate fresh ones with: node -e "console.log(require('ethers').Wallet.createRandom())"

BESU_RPC_URL=http://localhost:8545

ADMIN_PRIVATE_KEY=
ADMIN_ADDRESS=

ANSON_PRIVATE_KEY=
ANSON_ADDRESS=

BEATRICE_PRIVATE_KEY=
BEATRICE_ADDRESS=
