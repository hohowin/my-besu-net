{
  "name": "besu-digital-asset-demo-contracts",
  "version": "0.1.0",
  "private": true,
  "description": "Trimmed ERC-3643 (T-REX) contract suite for besu-digital-asset-demo (Phase 2)",
  "type": "commonjs",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "typecheck": "tsc --noEmit",
    "deploy:besu": "hardhat run scripts/deploy.ts --network besu"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "@types/node": "^20.14.0",
    "hardhat": "^2.22.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.5.0"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.2",
    "dotenv": "^16.4.5"
  }
}
